#!/bin/bash
# Join this machine to the Hecate cluster
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

set -e

echo "Joining cluster at {{ k3s_server_url }}..."
curl -sfL https://get.k3s.io | K3S_URL={{ k3s_server_url }} K3S_TOKEN={{ k3s_token }} sh -s - agent

echo ""
echo "Done! This node should now appear in: kubectl get nodes"
